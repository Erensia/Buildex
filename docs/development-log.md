# Buildex 개발 로그

## 2026-07-22 — 회원 빌드 즐겨찾기와 3인 파티 구성

- 저장 빌드를 즐겨찾기에 등록하고, 본인이 소유한 세 개의 저장 빌드를 3인 파티 슬롯에 배치·저장하는 회원 전용 기능을 추가했다.
- 파티는 계산식 또는 DPS 결과를 만들지 않으며, 빌드 조합 관리만 제공한다. 즐겨찾기·파티 API는 서버에서 빌드 소유권을 확인한다.

## 2026-07-22 — 기본 스탯과 에코 부옵션 합산 정정

- 치명타 확률 5%, 치명타 피해 150%, 공명 효율 100%의 글로벌 기본값을 합산 전 기준으로 고정했다.
- 무기·에코 주옵션·부옵션이 해당 스탯을 하나라도 제공할 때 기존 기본값을 덮어쓰던 문제를 수정하고, 에코 부옵션 합산 회귀 테스트를 추가했다.

## 2026-07-22 — 공개 무기 정보 카드

- 공개 캐릭터 화면에 현재 릴리스의 무기를 개별 카드로 표시했다. 카드에는 무기 종류와 등록된 기초 공격력, 치명타, 공격력 보조 스탯을 제공한다.

## 2026-07-22 — 공개 정보 표기 정정

- 공개 예시 카드의 자니 표기를 젠니로 정정했다.
- 치사와 루실라의 역할을 각각 `서브 딜러·생존 치료`, `서브 딜러·빠른 협주`로 보정하는 데이터 마이그레이션을 추가했다.
- 공개 캐릭터 정보에서 내부 무기 타입 키 대신 직검·대검·권갑·증폭기·권총의 한글 명칭을 표시한다.
- 기존 공개 릴리스에 남은 깨진 역할 문자열을 직접 복구하는 후속 마이그레이션을 추가했다.
- 역할 문자열 복구에 사용한 유니코드 이스케이프의 오타를 후속 마이그레이션에서 바로잡았다.
- 루실라의 누락된 증폭기 무기 타입을 복구하고, `amplifier`와 `rectifier`로 나뉜 기존 증폭기 데이터를 공개 목록에서 같은 무기 종류로 처리했다. 청음(Augment)을 증폭기 데이터에 추가했다.

## 2026-07-22 — 공개 탐색·회원 개인화 하이브리드 범위 확정

- 제품 코어는 명조 단일 게임의 빌드 플래너로 유지한다. 비회원에게는 현재 공개 릴리스의 캐릭터·무기·에코 세트 및 예시 빌드 결과를 먼저 제공해 제품 가치를 확인할 수 있게 한다.
- 빌드 계산·저장·즐겨찾기와 3인 파티 구성 관리는 회원 전용으로 둔다. 파티 기능은 저장 빌드 또는 캐릭터를 슬롯에 배치하는 관리 도구이며, 검증되지 않은 대미지·로테이션 계산은 범위에서 제외한다.
- 공개 탐색과 회원 개인화가 끝나면 지원 캐릭터 확대, 계산 정확도, 게임 데이터 운영 고도화를 다음 목표로 진행한다.

## 2026-07-22 — 저장 빌드 비교의 응결 스탯 보완

- 결과 카드에는 표시되지만 저장 빌드 비교에서 빠져 있던 응결 피해 보너스를 비교 항목에 추가했다.
- 저장 빌드 비교는 현재 지원 중인 용융·회절·응결 피해 보너스를 모두 같은 기준으로 표시한다.

## 2026-07-22 — 릴리스 발행 완전성 검증 강화

- 발행 검증에서 누락된 행의 데이터 버전도 오류로 처리하고, 릴리스 출처의 존재를 확인하도록 보강했다.
- 모든 에코와 에코 세트의 소속 관계, 1·3·4 코스트 주옵션, 각 캐릭터 무기 타입을 지원하는 무기의 존재를 확인한다. 불완전한 초안은 공개할 수 없다.

## 2026-07-22 — 계산식 버전별 저장 결과 재현

- 과거 빌드의 `formula_version`이 현재 계산식 번들과 다를 때, 플래너가 현재 식으로 임의 재계산하지 않고 서버에 저장된 `calculated_result`를 표시하도록 변경했다.
- 이 상태에서는 결과 패널에 저장 계산식 버전을 표시하고 개선 추천을 숨긴다. 사용자가 `현재 식으로 재계산`을 명시적으로 실행할 때만 현재 계산식 결과와 버전으로 저장 빌드를 갱신한다.

## 2026-07-22 — 릴리스 기준 출처·검증일 표시

- 플래너 데이터 API가 게임의 단일 대표 URL 대신 선택된 데이터 릴리스의 출처 목록(`sourceManifest`)과 검증 날짜를 전달하도록 변경했다.
- 플래너는 데이터 버전, 검증 날짜, 릴리스별 출처 링크를 함께 표시한다. 따라서 새 릴리스를 발행한 뒤에도 이전 게임 메타데이터 URL이 표시되는 문제가 없다.

## 2026-07-17 — 플래너 데이터 표시와 부옵션 규칙 정정

- 게임 데이터 API 오류 응답이 플래너 상태에 들어와 화면이 중단되던 문제를 방어하고, 오류 메시지를 사용자에게 표시하도록 수정했다.
- 등급 달성·추천 개선 안내에서 공격력은 수치로, 치명타·공명 효율·피해 보너스는 `%` 단위로 표시하도록 통일했다.
- 에코 부옵션에서 속성 피해 보너스를 제거하고 일반 공격·강공격·공명 스킬·공명 해방 피해 보너스로 교체했다. 공격력%·HP% 및 유형별 피해 보너스는 동일한 실제 8단계 롤 범위(`6.4%`~`11.6%`)만 허용한다.

## 2026-07-17 — 문서와 구현 정합성 점검

- 등급 기준은 최초의 창리 전용 구현에서 자니·히유키까지 확장됐다. 요구사항과 기본 설계의 지원 범위를 이에 맞춰 정정했다.
- 데이터 릴리스 식별자는 저장하지만, 당시 릴리스로 빌드를 읽고 현재 릴리스로 명시적으로 복제·재계산하는 사용자 흐름은 아직 구현 대상임을 문서에 명시했다.

## 2026-07-17 — 빌드 재현성과 릴리스 안전성 보강

- 저장 빌드를 열 때 해당 빌드의 `data_release_id`로 공개·보존 릴리스 데이터를 읽고, 수정 API도 동일 릴리스를 기준으로 입력을 다시 검증하도록 변경했다. 최신 데이터로 옮기는 작업은 현재 공개 데이터로의 복제로 분리했다.
- 계산식 버전은 클라이언트 입력이 아닌 서버의 `FORMULA_VERSION`으로 기록하도록 통일하고, 현재 명조 3.5 계산식 번들로 갱신했다.
- 게임당 공개 릴리스는 하나만 허용하는 부분 고유 인덱스를 추가했다. Publish는 초안 상태를 조건으로 전환하고 충돌 시 409를 반환한다.

## 2026-07-17 — 파티 계산 범위 결정

- 파티 기능의 첫 단계는 메인 캐릭터·지원 캐릭터 편성과 조건부 버프를 바탕으로 한 최종 스탯 합산으로 정했다. 결과 화면은 버프 제공자, 조건, 적용 여부를 근거로 표시한다.
- 전체 로테이션 DPS는 스킬·적·시간축 데이터와 검증 체계가 준비된 뒤 별도 단계로 진행한다. 현 단계에서 DPS 수치를 추정하지 않는다.

## 2026-07-17 — 파티 최종 스탯 계산 1차 구현

- 플래너에 지원 캐릭터 2명 편성을 추가하고, 메인 캐릭터와 편성된 지원 캐릭터의 조합에 따라 조건부 파티 버프를 적용 가능/미적용으로 판정한다.
- 결과는 버프 제공자, 버프 조건, 적용 여부와 미적용 사유를 함께 표시한다. 저장 API도 동일한 파티 규칙과 현재 데이터 릴리스의 캐릭터 존재 여부를 다시 검증한다.
- 개인 전용무기 버프는 파티 버프와 분리해 기존처럼 무기 조건으로 판정한다. 파티 규칙의 공식 지원 범위와 게임 데이터 릴리스 귀속은 후속 정합성 작업으로 관리한다.

## 2026-07-17 — 파티 계산 1차 검수 후 정합성 보강

- 파티 버프를 코드 상수에서 `party_buffs` 릴리스 데이터로 이전했다. 초안 복제와 발행 검증은 파티 버프를 함께 복제하고, 대상·제공 캐릭터 및 수치 형식을 검사한다.
- 새 환경에서 재현 가능한 공식 지원 파티 범위는 초기 시드가 포함한 장리–루파–브렌트로 확정했다. 히유키 관련 등급 계산식은 유지하되, 파티 버프 지원으로 문서화하지 않는다.
- 편성이 충족된 버프와 실제 최종 스탯에 반영된 버프를 구분해 표시하도록 안내를 추가했다. 파티 편성은 최대 2명의 지원 캐릭터를 허용한다.

## 2026-07-17 — 릴리스 기반 게임 데이터 관리 착수

- 패치 데이터를 행 단위로 즉시 덮어쓰지 않고, `draft` → `published` → `superseded` 상태를 갖는 `game_data_releases` 스냅샷으로 관리하기로 결정했다.
- 기존 명조 3.5 데이터를 첫 공개 릴리스로 이관하고, 캐릭터·무기·에코·세트·주옵션을 `release_id`로 귀속하는 마이그레이션을 추가했다.
- 플래너 조회와 서버 측 빌드 검증·저장이 현재 공개 릴리스만 사용하도록 전환했다. 새 저장 빌드는 `data_release_id`를 고정해 향후 패치와 무관하게 계산 기준을 추적한다.
- 다음 작업은 관리자 초안 복제·발행 API, 발행 전 도메인 검증, 관리자 화면의 draft/publish 흐름이다.

## 2026-07-17 — 관리자 draft/publish 워크플로 구현

- 관리자 화면을 릴리스 대시보드로 변경했다. 현재 공개 릴리스와 초안·과거 릴리스를 구분해 보여 주고, 공개본을 복제해 새 버전의 초안을 만든다.
- 기존 JSON 편집기는 유지하되 선택한 `draft`에만 저장하도록 제한했다. 공개·과거 릴리스는 읽기 전용이므로 운영 데이터가 행 단위로 즉시 변경되지 않는다.
- 초안 발행 전 서버가 데이터 최소 구성, 릴리스 버전·검증일 일관성, 에코 세트 소속 관계를 검사한다. 발행 확인 뒤 새 릴리스 공개, 이전 릴리스 보존, 현재 게임 릴리스 포인터 변경은 하나의 트랜잭션으로 수행한다.

## 2026-07-17 — 히유키·치사·루실라 응결 파티 및 에코 데이터 정비

- 히유키를 기준으로 치사·루실라 조합의 조건부 버프, 등급 목표치, 저장 빌드 재계산 로직을 추가했다. 응결 피해 보너스를 독립 스탯으로 계산·표시·검증한다.
- 히유키·치사·루실라, 전용 무기, `소리 없이 내려앉은 기도의 눈` 에코 세트와 개별 에코·세트 구성을 로컬 게임 데이터에 등록했다. 에코 세트 5세트 효과의 치명타 확률도 계산에 반영한다.
- 부옵션 입력을 자유 수치 입력에서 실제 에코 롤 수치 선택 방식으로 바꾸고, 에코당 최대 5개 및 유효 롤 값 검증을 적용했다.
- 에코 주옵션과 추천 문구의 스탯 레이블을 한글화했다. 응결 파티 관련 조건부 효과는 `서리 효과`라는 표기로 통일했다.
- PowerShell 입력 과정에서 발생한 한글 물음표 저장 문제를 복구했다. 캐릭터·무기·에코·세트 이름과 `소리 없이 내려앉은 기도의 눈` 5세트 조건을 정상 한글 및 JSON 객체 형태로 정정했다.
- `pnpm lint`, `pnpm test`, `pnpm build`를 통과했다.

## 2026-07-16 — 관리자 게임 데이터 운영

- `users.role`과 관리자 부트스트랩(`ADMIN_EMAIL`)을 추가했다. 일반 회원가입 계정은 기본 역할 `user`로 생성되며, 관리자 API는 서버에서 역할을 다시 검사한다.
- `/admin`과 `/api/admin/game-data`를 추가해 게임, 캐릭터, 무기, 에코, 에코 세트, 주옵션, 세트 구성을 등록·수정·삭제할 수 있게 했다.
- 입력은 Zod로 데이터 버전, 검증 날짜, 출처 URL 및 식별자를 검증한다. 기존 레코드를 불러와 JSON으로 편집할 수 있고, 같은 식별자로 저장하면 갱신된다.
- `pnpm lint`, `pnpm build`, `pnpm test`를 통과했다.

## 2026-07-16 — 기반 구축

- Next.js, PostgreSQL, Drizzle ORM, Auth.js Credentials 기반을 구성했다.
- 사용자, 인증 식별자, 게임 데이터, 빌드 프로필 스키마와 마이그레이션을 추가했다.
- 창리 기준 스탯 계산기와 단위 테스트를 추가했다.

## 2026-07-16 — 빌드 저장

- 로그인 사용자의 빌드 목록 조회, 생성, 삭제 API를 구현했다.
- 서버에서 빌드 입력을 검증하고 계산 결과와 버전을 함께 저장하도록 했다.

## 2026-07-16 — 데이터 선택 및 편집

- `/api/build-data`로 DB 기반 캐릭터·무기·에코 세트·주옵션 선택 데이터를 제공한다.
- 캐릭터에 맞는 무기 타입만 선택하도록 빌드 UI를 변경했다.
- 저장 빌드를 불러오고 수정(PUT)·삭제할 수 있게 했다.
- 문서 네 개를 UTF-8 한국어 원문으로 정규화했다.

## 2026-07-16 — MVP 에코 선택지 보완

- 1·3 코스트 에코와 세트 연결 데이터를 추가해 4-3-3-1-1 빌드 슬롯을 모두 선택할 수 있게 했다.
- 저장 입력에서 5개 슬롯, 슬롯별 코스트, 에코 선택 여부를 검증하도록 강화했다.
- 에코 데이터는 출처 URL과 스냅샷을 함께 기록해 다음 갱신의 기준으로 사용한다.

## 2026-07-16 — 에코 세트 효과 계산

- 에코 슬롯마다 세트를 선택해 2세트/5세트 조합을 표현할 수 있게 했다.
- 2세트 효과는 자동 적용하고, 조건이 있는 5세트 효과는 사용자 확인 후 클라이언트와 서버 계산에 동일하게 반영한다.

## 2026-07-16 — 빌드 참조 무결성 검증

- 서버가 무기 타입, 에코 코스트, 에코-세트 소속, 주옵션 가능 여부를 DB 기준으로 검증하도록 했다.
- 화염 발톱 세트의 1·3 코스트 선택지와 연결 데이터를 보충했다.

## 2026-07-16 — 에코별 부옵션 및 조건부 버프

- 부옵션을 에코별로 입력·저장·복원하도록 변경했다.
- 창리 전용무기 버프는 해당 무기 장착 시에만 표시하고 서버 계산에도 반영한다.

## 2026-07-16 — 등급 기반 부옵션 추천

- 창리 빌드는 다음 등급에 도달하기 위해 부족한 스탯을 결과 패널에서 바로 확인할 수 있게 했다.

## 2026-07-16 — 로컬 환경 설정 안정화

- `.env.local`에 로컬 데이터베이스 연결 정보와 고정 `NEXTAUTH_SECRET`, `NEXTAUTH_URL`을 추가했다.
- JWT 세션 복호화 오류를 방지하기 위해 NextAuth secret을 개발 서버 재시작 시에도 유지하도록 했다.
- `drizzle.config.ts`가 `.env.local`을 자동으로 읽도록 `dotenv`를 추가해, 마이그레이션 시 별도의 환경변수 명령이 필요 없게 했다.

## 2026-07-16 — 서버 주옵션 계산 데이터화

- 빌드 저장·수정 API가 주옵션 수치를 코드에 고정하지 않고, 검증에 사용한 `echo_main_stats` 게임 데이터에서 읽도록 통일했다.
- 서버 계산용 에코 스탯 소스를 공용 모듈로 분리하고, 데이터 값 변경이 계산에 반영되는 단위 테스트를 추가했다.

## 2026-07-16 — 저장 빌드 비교

- 현재 편집 중인 빌드와 저장 빌드를 선택해 공격력, 치명타, 공명 효율, 융해 피해 보너스 및 증감값을 한눈에 비교할 수 있게 했다.

## 2026-07-16 — 에코 부옵션 품질 검증

- 에코별 부옵션을 지원하는 항목으로 제한하고 중복 입력을 서버에서 거부하도록 검증을 강화했다.
- 각 에코의 유효 부옵션 개수와 치명타·속성 피해 중심의 비교용 품질 점수를 표시했다.

## 2026-07-16 — 등급 목표 행동 추천

- 다음 등급의 미달 스탯을 주옵션 교체 가능 여부와 연결해, 교체 후보 또는 우선 보강할 부옵션으로 안내하도록 확장했다.

## 2026-07-16 — 게임 데이터 운영 정보

- 빌드 데이터 API와 플래너에 현재 게임 데이터 버전, 검증 날짜 및 출처 링크를 노출했다.
- 패치 데이터 추가·검증·버전 갱신 절차를 문서화해 데이터 확장 시 계산 기준과 추적 정보를 유지하도록 했다.

## 2026-07-16 — 플래너 사용성 정리

- 캐릭터 변경 시 호환 무기 선택을 초기화해 표시와 저장 상태가 엇갈리지 않게 했다.
- 빌드 복제, 삭제 확인, 한국어 중심 문구 및 접근성 있는 빌드 이름 입력을 추가했다.

## 2026-07-16 — `/build` 화면 가독성 및 UI 개선

- 빌드 입력을 캐릭터·무기, 에코 세팅, 부옵션의 3단계 카드 구조로 재구성해 입력 순서를 명확히 했다.
- 결과 패널을 고정형 요약 영역으로 강화하고, 핵심 스탯·등급 목표·개선 추천·저장 기능을 시각적으로 분리했다.
- 저장 빌드 비교와 목록의 증감 표시를 정리하고, 폼 요소의 포커스 상태·모바일 레이아웃·한국어 안내 문구를 개선했다.
- `pnpm lint` 및 `pnpm build`로 타입 검사와 프로덕션 빌드를 검증했다.

## 보류 항목

- Google, Kakao, Naver 소셜 로그인
- 파티 시뮬레이션
- 자니·히유키 외 캐릭터의 등급 기준 및 범용 추천
