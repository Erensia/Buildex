# Buildex 개발 기록

## 2026-07-16 — 서비스 기획 초안

- 비회원에게 게임 카테고리, 캐릭터, 장비, 무기 정보 열람을 공개하기로 정의했다.
- 회원 전용 기능을 캐릭터 스펙 확인·저장, 캐릭터 즐겨찾기, 파티 조합 시뮬레이션으로 정리했다.
- 자체 이메일/비밀번호 가입부터 시작하되, 외부 인증 제공자를 추가할 수 있도록 사용자와 인증 수단을 분리하는 방향을 결정했다.
- `basic-design.md`, `requirements.md`, `erd.md`에 서비스 개요, 요구사항, 데이터 모델 초안을 기록했다.

## 2026-07-16 — 명조 MVP 범위 확정

- 첫 지원 게임은 **명조: 워더링 웨이브**로 결정했다. 젠레스 존 제로는 공식 앱의 지원 범위가 넓어, 첫 MVP의 차별화 대상에서는 제외한다.
- 콘텐츠 데이터는 공식 버전 공지·패치 노트와 게임 내 공개 수치를 기준으로 운영한다. 초기에는 관리자 수동 입력·검수 방식으로 갱신하며, 게임 클라이언트 분석·비공개 API 호출·계정 자동 연동·미출시 정보는 범위에서 제외한다.
- MVP는 실전 DPS나 자동 로테이션 계산기가 아니라, **스탯과 조건부 버프를 투명하게 합산하는 빌드·파티 비교 도구**로 정의했다.
- 캐릭터 평가는 `준종결`, `종결`, `극종결`의 세 기준으로 제공한다. 각 기준은 캐릭터·무기·파티·운용 조건별 목표 스탯과 필수 조건으로 관리한다.
- 초기 지원 대상은 주요 딜러·범용 서포터를 정확하게 지원한 뒤 확대하는 방식으로 정했다. 첫 검증 파티는 **장리–루파–브랜트(장루브)**, 두 번째 확장 파티는 **젠니–페비–벨리나**다.
- 첫 구현은 장루브 파티를 기준으로 한다. 장리의 기준 빌드는 **0공명 + 전용 무기**로 시작하고, 다른 무기 및 고공명 변형은 후속 지원한다.
- 사용자는 캐릭터·무기·에코를 수동으로 입력한다. 에코 세트와 주옵은 목록에서 선택하고, 에코 레벨은 만렙 고정으로 처리한다. 부옵은 최대 5개를 수치와 함께 입력한다.
- 에코 5개 묶음은 이름을 붙인 빌드 프리셋으로 저장·불러올 수 있게 한다. 결과에는 개인 최종 스탯, 파티 버프 적용 전·후 차이, 목표 등급 판정, 에코별 교체 우선순위를 표시한다.

## 다음 기획 작업

- 장루브 파티의 실제 스탯 계산식, 버프 발동 조건, 목표 등급별 수치를 정의한다.
- 회원가입 입력 항목, 계정 복구, 탈퇴 및 개인정보 처리 정책을 구체화한다.
- 장루브 MVP의 화면 와이어프레임과 API 명세를 작성한다.

## 2026-07-16 — 프로젝트 기반 구축 완료

- `Erensia/Buildex` 저장소에 Next.js App Router, TypeScript, Tailwind CSS 기반 프로젝트를 초기화하고 첫 커밋 `e5944f2`를 `main`에 반영했다.
- Docker PostgreSQL 16 구성, Drizzle ORM 스키마와 초기 마이그레이션을 추가했다. 실제 스키마는 `users`, `auth_identities`, `games`, `characters`, `weapons`, `echoes`, `build_profiles`로 구성된다.
- 이메일 중복 검사, Zod 입력 검증, bcrypt 비밀번호 해시를 사용하는 회원가입 API와 Auth.js Credentials 로그인 흐름을 구현했다.
- 에코 5개 빌드 입력 검증과 Vitest 테스트를 추가했다. ESLint, 단위 테스트, 프로덕션 빌드, Docker Compose 설정 검증을 통과했다.
- 기획 문서와 실제 구현의 차이를 반영해 ERD의 `equipment`를 `echoes`로, `character_specs`를 `build_profiles`로 정정했다. 즐겨찾기와 파티 시뮬레이션 모델은 아직 추가하지 않았다.

## 다음 개발 작업

- 명조 게임 데이터(장리, 루파, 브랜트, 무기, 에코 세트)와 데이터 버전 시드 추가
- ✅ 독립 스탯 계산 엔진, 파티 버프 조건, 목표 등급 판정 규칙 구현 및 단위 테스트
- 빌드 입력·저장·결과 비교 화면 구현
- 세션 표시, 로그아웃, 보호 경로와 원래 경로 복귀 구현

## 2026-07-16 — 장루브 계산 기반 구현

- `src/lib/formula/build-calculator.ts`에 캐릭터·무기·에코의 기초 공격력, 공격력%, 고정 공격력을 합산하는 독립 계산기를 추가했다. 치명타, 공명 효율, 융해 피해 보너스, 공명 스킬 피해 보너스도 같은 결과 모델로 반환한다.
- 파티 버프는 자동으로 가정하지 않는다. UI가 버프 발동 조건을 확인한 뒤 활성 버프 ID를 넘길 때만 합산하므로, 결과의 적용 전제와 계산 근거를 표시할 수 있다.
- 장리 0공명 + 전용 무기 기준의 `준종결`·`종결`·`극종결` 목표 스탯을 데이터 규칙으로 분리했다. 규칙 평가기는 달성한 최고 등급과 다음 등급에 부족한 항목을 반환한다.
- 계산식과 버프 적용, 등급 판정의 단위 테스트를 추가했다. 장리·루파·브랜트의 실제 캐릭터·무기·에코 데이터 시드는 다음 작업에서 별도로 넣고, 그때 패치 기준 수치도 이 규칙 데이터와 함께 검수한다.
